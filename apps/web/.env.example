# In dev, the web app talks to the api via Vite's `/api` proxy so the api
# looks same-origin and cookies just work. In prod set this to the api's
# absolute URL (and configure SameSite=None;Secure cookies there).
VITE_API_BASE_URL=/api
